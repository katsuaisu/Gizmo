{
    "buildCommand": "bun run build && bun run prerender",
    "outputDirectory": "dist/client",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}